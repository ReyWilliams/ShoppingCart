Extract files from zip archive to a folder by itself.
Maintain the names given and do not change the hierarchy.
Ensure that the jar file is in the dame directory as the txt files needed for input
When those considerations are taken and met go ahead and run the jar file by using the terminal
and navigating to the folder and then run "java -jar project.jar"